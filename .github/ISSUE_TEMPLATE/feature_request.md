---
name: Feature request
about: Propose new behavior or API
labels: enhancement
---

**The problem this solves**
What can't you do today, or what is painful?

**Proposed shape**

```ts
// what the API would look like in use
```

**How EF Core (or another ORM) handles this**
rnxORM follows EF Core's model where sensible. Prior art helps scope the design.

**Scope notes**
Current provider focus is SQL Server + PostgreSQL (see issues #8/#12 for the
MySQL/MariaDB positioning). Features that only make sense on other engines
will likely be deferred.
