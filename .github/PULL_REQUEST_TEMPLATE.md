## What this changes

Closes #

## Evidence

- [ ] `npm test` green (mock suite)
- [ ] `npm run test:integration` green against real databases, or CI's
      integration workflow covers it
- [ ] New behavior has a test that fails without the change
- [ ] `npm run lint` reports 0 errors and `npm run build` is clean

## Docs

- [ ] README / CHANGELOG updated if behavior or claims changed
      (README claims must stay evidence-based. Link the proving test)
